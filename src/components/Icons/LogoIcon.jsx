import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

import { faCirclePlay } from '@fortawesome/free-solid-svg-icons'

const LogoIcon = ({ size }) => {
  return (
    <FontAwesomeIcon icon={faCirclePlay} size={size} />
  )
}

export default LogoIcon
